version: 1
formatters:
  simple:
    format: "%(asctime)s - %(levelname)s - %(message)s"
handlers:
  console:
    class: logging.StreamHandler
    formatter: simple
    level: INFO
loggers:
  app:
    handlers: [console]
    level: INFO
    propagate: no
root:
  handlers: [console]
  level: INFO
